<Window x:Class="WildcardSingularity.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WildcardSingularity"
        Height="560" Width="520"
        MinHeight="440" MinWidth="400"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0">

            <!-- Tab 1: Files -->
            <TabItem Header="Files">
                <Grid Margin="0,8,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Folder row -->
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 Text="{Binding FolderPath, Mode=OneWay}"
                                 IsReadOnly="True"
                                 VerticalContentAlignment="Center"
                                 Margin="0,0,6,0"/>
                        <Button Grid.Column="1"
                                Content="Browse…"
                                Command="{Binding BrowseFolderCommand}"
                                IsEnabled="{Binding IsNotRunning}"
                                Width="80"/>
                    </Grid>

                    <!-- Enable / Disable all -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,4">
                        <Button Content="Enable All"
                                Command="{Binding SelectAllCommand}"
                                Width="90" Margin="0,0,6,0"/>
                        <Button Content="Disable All"
                                Command="{Binding DeselectAllCommand}"
                                Width="90"/>
                    </StackPanel>

                    <!-- File list -->
                    <ListView Grid.Row="2"
                              ItemsSource="{Binding Files}"
                              Margin="0,0,0,10"
                              SelectionMode="Single">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="4,2"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                        <Setter Property="Background" Value="#D6EAF8"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="42"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0"
                                             Text="{Binding LineCount, UpdateSourceTrigger=LostFocus}"
                                             VerticalContentAlignment="Center"
                                             Margin="0,0,6,0"/>
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding FileName}"
                                               VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- Interval -->
                    <Grid Grid.Row="3" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="70"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0"
                               Content="Interval (seconds):"
                               VerticalAlignment="Center"
                               Padding="0,0,6,0"/>
                        <TextBox Grid.Column="1"
                                 VerticalContentAlignment="Center"
                                 IsEnabled="{Binding IsNotRunning}"
                                 Text="{Binding IntervalSeconds, UpdateSourceTrigger=LostFocus}"/>
                    </Grid>

                    <!-- Start / Stop + countdown -->
                    <Grid Grid.Row="4" Margin="0,0,0,8">
                        <TextBlock HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibility}}">
                            <Run Text="Next run in: "/>
                            <Run Text="{Binding CountdownSeconds}"/>
                            <Run Text="s"/>
                        </TextBlock>
                        <Button HorizontalAlignment="Right"
                                Width="80"
                                Command="{Binding StartCommand}"
                                Visibility="{Binding IsNotRunning, Converter={StaticResource BoolToVisibility}}">
                            Start
                        </Button>
                        <Button HorizontalAlignment="Right"
                                Width="80"
                                Command="{Binding StopCommand}"
                                Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibility}}">
                            Stop
                        </Button>
                    </Grid>
                </Grid>
            </TabItem>

            <!-- Tab 2: Search & Replace -->
            <TabItem Header="Search &amp; Replace">
                <Grid Margin="0,8,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Preset toolbar -->
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0"
                               Content="Preset:"
                               VerticalAlignment="Center"
                               Padding="0,0,6,0"/>
                        <ComboBox Grid.Column="1"
                                  IsEditable="True"
                                  ItemsSource="{Binding PresetList}"
                                  DisplayMemberPath="Name"
                                  Text="{Binding PresetName, UpdateSourceTrigger=PropertyChanged}"
                                  VerticalContentAlignment="Center"
                                  Margin="0,0,6,0"/>
                        <Button Grid.Column="2"
                                Content="Save"
                                Command="{Binding SavePresetCommand}"
                                Width="55" Margin="0,0,4,0"/>
                        <Button Grid.Column="3"
                                Content="Load"
                                Command="{Binding LoadPresetCommand}"
                                Width="55" Margin="0,0,4,0"/>
                        <Button Grid.Column="4"
                                Content="Delete"
                                Command="{Binding DeletePresetCommand}"
                                Width="55"/>
                    </Grid>

                    <!-- Rules list -->
                    <ListView Grid.Row="1" ItemsSource="{Binding Rules}">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="2,2"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Search" Width="185">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding SearchPattern, UpdateSourceTrigger=PropertyChanged}"
                                                     Width="173"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Replace" Width="185">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Replacement, UpdateSourceTrigger=PropertyChanged}"
                                                     Width="173"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="50">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="×"
                                                    FontSize="14"
                                                    Width="30"
                                                    Command="{Binding DataContext.RemoveRuleCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                    CommandParameter="{Binding}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <!-- Add rule -->
                    <Button Grid.Row="2"
                            Content="+ Add Rule"
                            Command="{Binding AddRuleCommand}"
                            HorizontalAlignment="Left"
                            Width="90"
                            Margin="0,6,0,4"/>
                </Grid>
            </TabItem>

        </TabControl>

        <!-- Status bar -->
        <StatusBar Grid.Row="1" Margin="-12,-4,-12,-12" Padding="8,4">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
